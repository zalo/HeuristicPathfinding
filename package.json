{
	"scripts": {
		"build": "esbuild ./src/main.js --bundle --sourcemap --format=esm --outdir=./build",
		"buildMinify": "esbuild ./src/main.js --bundle --minify --format=esm --outdir=./build"
	},
	"dependencies": {
		"esbuild": "^0.14.23",
		"three": "^0.147.0"
	}
}
